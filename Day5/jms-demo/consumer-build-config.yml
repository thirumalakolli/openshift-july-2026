apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: jms-consumer 
  labels:
    name: jms-consumer 
spec:
  triggers:
  - type: ConfigChange
  source:
    type: Git
    git:
      uri: "https://github.com/tektutor/openshift-feb-2026.git"
      ref: main
    contextDir: "Day5/jms-demo/consumer"
  strategy:
    type: Docker
  output:
    to:
      kind: ImageStreamTag
      name: 'jms-consumer:latest'
